# CLAUDE.md

AI エージェント（Claude など）がこのリポジトリで作業する際のガイドラインをまとめます。

---

## ドキュメント管理ルール

本プロジェクトでは以下のドキュメント体系で管理します。各ファイルの役割を理解した上で、適切なファイルに情報を記録してください。

| ファイル | 役割 | 書くべき内容 |
|---|---|---|
| `docs/development.md` | 開発手順書 | 環境構築・コマンド・デプロイ手順など、開発者が手を動かす際に参照する情報 |
| `docs/plan.md` | 実装計画メモ | 決定前のアイデア・選択肢・議論の記録。探索の場として自由に使う |
| `docs/spec.md` | 仕様書 | `plan.md` での議論を経て**確定した**仕様のみ。未確定事項は書かない |
| `docs/todo.md` | TODO リスト | 実装タスクの一覧。完了済みタスクは削除せずチェックを付ける |
| `docs/knowledge.md` | 汎用ナレッジ | プロジェクト固有でない知見。他プロジェクトへ流用できる内容に限定 |
| `docs/adr/` | ADR（意思決定記録） | 特徴量・ハイパーパラメータ・戦略などの採否判断と根拠（Context/Decision/Consequences）。1決定＝1ファイル。生の実験データ・グリッド表は書かない |
| `docs/experiments.md` | 実験ログ索引 | 日付・タイトル・結果・ADRへのリンクの1行のみ。判定が出た実験は必ずADR化し、詳細はここに書かない |

### 運用ルール

1. **plan.md → spec.md の流れを守る**
   - 仕様の検討は `plan.md` で行い、確定したものだけ `spec.md` に記録する。
   - `spec.md` に未確定の内容を書かない。

2. **knowledge.md はプロジェクト非依存に保つ**
   - プロジェクト固有の情報（データソースのURL、モデルのパスなど）は書かない。
   - 汎用的な技術知見・ドメイン知識のみ記録する。

3. **todo.md の完了タスクの扱い**
   - 個々のタスクが完了したら削除せず `- [x]` でチェックを付けて残す。
   - **セクション内の全タスクが完了したら、そのセクションごと削除する。**

4. **development.md は実行可能な状態に保つ**
   - 手順が古くなったらすぐ更新する。
   - コマンドは実際に動作するものだけ記載する。

5. **実験は判定が出たらADR化する**
   - 特徴量追加・ハイパーパラメータ変更・戦略変更などを検証し、採用/不採用の判定が出たら `docs/adr/` にADRを1本作成する（テンプレートは `docs/adr/README.md` 参照）。
   - `docs/experiments.md` には日付・タイトル・結果・ADRへのリンクの1行だけを追加する。生データ・グリッド表・所感の詳細はADRにも書かず、必要な数値だけをConsequencesに要約する。
   - 過去の決定を覆す場合は新しいADRを作成し、旧ADRのStatusを `Superseded by ADR-NNNN` に書き換える（削除しない）。
   - 判定に至らなかった探索的な計測はADR化・記録しない（`git log` から辿れれば十分）。

---

## コーディング規約

> TODO: コーディング規約をここに記載する

## コミットメッセージ規約

> TODO: コミットメッセージ規約をここに記載する
